# Telephony Samples

Add optional WAV clips in this directory to exercise the telephony harness with
recorded audio. The harness also generates a synthetic tone sample when no file
is provided, so storing binaries here is not required.
